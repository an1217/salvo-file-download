The environment variables rustclient specifies the location where the download is from the server, and rustserver specifies the server path to be timed

The server compresses and encrypts the specified directory on a regular basis

The client specifies the time to download and decrypt the data into a zip
The logs are in the specified directory